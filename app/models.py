"""
Legacy compatibility module.

The canonical data model lives in the `app.models` package and follows
Master_Prompt_v1.0: PostgreSQL + SQLAlchemy models, API-first backend.
Do not add new SQLModel/SQLite entities here.
"""
